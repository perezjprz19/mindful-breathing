let timeoutId, breathingCycle = [{ phase: "inhale", duration: 5e3, message: "Breathe in deeply through your nose, feeling your lungs expanding." }, { phase: "hold", duration: 5e3, message: "Pause and feel your body still." }, { phase: "exhale", duration: 5e3, message: "Breathe out through your mouth, letting go of unnecessary tension." }], currentPhaseIndex = 0, isRunning = !1, toggleSessionBtn = document.getElementById("toggleSession"); function toggleSession() { !1 === isRunning ? (toggleSessionBtn.innerText = "Stop Session", startBreathing(), isRunning = !0) : (toggleSessionBtn.innerText = "Start Session", stopBreathing(), isRunning = !1) } function startBreathing() { clearTimeout(timeoutId), currentPhaseIndex = 0, currentPhase(), console.log("You've started the breathing exercise!") } toggleSessionBtn.addEventListener("click", toggleSession); let intervalId, displayCurrentPhase = document.getElementById("displayCurrentPhase"); function currentPhase() { displayCurrentPhase.innerText = breathingCycle[currentPhaseIndex].message, updateCircle(breathingCycle[currentPhaseIndex].phase), timeoutId = setTimeout((() => { currentPhaseIndex = (currentPhaseIndex + 1) % breathingCycle.length, currentPhase() }), breathingCycle[currentPhaseIndex].duration) } function holdPhaseCountdown() { let e = breathingCycle[currentPhaseIndex].duration / 1e3, n = document.querySelector("#displayCountdown"); n.innerText = e, intervalId = setInterval((() => { e--, console.log(e), n.innerText = e, 0 === e && (clearInterval(intervalId), n.innerText = "", console.log("the timer has reached 0")) }), 1e3) } const circle = document.querySelector(".circle"); function updateCircle(e) { "inhale" === e ? (circle.classList.remove("exhale"), circle.classList.toggle("inhale")) : "hold" === e ? holdPhaseCountdown() : (circle.classList.remove("inhale"), circle.classList.toggle("exhale")) } function stopBreathing() { displayCurrentPhase.innerText = "Your session has ended.", displayCountdown.innerText = "", currentPhaseIndex = 0, clearTimeout(timeoutId), clearInterval(intervalId), intervalId = null, circle.classList.remove("exhale"), circle.classList.remove("inhale"), console.log("You've stopped the breathing exercise!", intervalId) }