//Define the breathing cycle phases and duration
let timeoutId,breathingCycle=[{phase:"inhale",duration:5e3,message:"Breathe in deeply through your nose, feeling your lungs expanding."},{phase:"hold",duration:5e3,message:"Pause and feel your body still."},{phase:"exhale",duration:5e3,message:"Breathe out through your mouth, letting go of unnecessary tension."}],currentPhaseIndex=0,startButton=document.getElementById("startButton");function startBreathing(){clearTimeout(timeoutId),// stop any old loop
currentPhaseIndex=0,// reset to Inhale
startButton.disabled=!0,currentPhase(),console.log("You've started the breathing exercise!")}startButton.addEventListener("click",startBreathing);let intervalId,displayCurrentPhase=document.getElementById("displayCurrentPhase");function currentPhase(){displayCurrentPhase.innerText=breathingCycle[currentPhaseIndex].message,updateCircle(breathingCycle[currentPhaseIndex].phase),timeoutId=setTimeout((()=>{currentPhaseIndex=(currentPhaseIndex+1)%breathingCycle.length,currentPhase()}),breathingCycle[currentPhaseIndex].duration)}function holdPhaseCountdown(){let e=breathingCycle[currentPhaseIndex].duration/1e3,t=document.querySelector("#displayCountdown");//set countdown variable to hold duration in seconds
t.innerText=e,// displays count down duration
intervalId=setInterval((()=>{e--,//decrease countdown every second
console.log(e),t.innerText=e,//update the countdown value
0===e&&(clearInterval(intervalId),//clear the countdown once it has reached 0. 
t.innerText="",console.log("the timer has reached 0"))}),1e3)}
//Update size of circle
const circle=document.querySelector(".circle");function updateCircle(e){
//at inhale, scale twice the size
"inhale"===e?(circle.classList.remove("exhale"),circle.classList.toggle("inhale")):"hold"===e?
//at hold, hold the max size and show countdown by calling the appropriate function
holdPhaseCountdown():(
//at exhale, remove pulse animation & return to original size
circle.classList.remove("inhale"),circle.classList.toggle("exhale"))}
//Stop button logic to stop the breathing exercise
let stopButton=document.getElementById("stopButton");function stopBreathing(){startButton.disabled=!1,displayCurrentPhase.innerText="Your session has ended.",displayCountdown.innerText="",currentPhaseIndex=0,clearTimeout(timeoutId),clearInterval(intervalId),intervalId=null,circle.classList.remove("exhale"),circle.classList.remove("inhale"),console.log("You've stopped the breathing exercise!",intervalId)}stopButton.addEventListener("click",stopBreathing);